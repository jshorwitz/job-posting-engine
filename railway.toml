[build]
dockerfilePath = "Dockerfile"

[deploy]
# Long-lived scheduler: handles X posting, LinkedIn, engagement scans,
# MCP reply scanning, AND enrichment pipeline (Mon-Fri 9am ET).
# No cron needed — the scheduler polls every 60s internally.
startCommand = "doppler run -- python scripts/x_scheduler.py"
